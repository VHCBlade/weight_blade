name: weight_blade
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  animations: ^2.0.0
  carousel_slider: ^4.2.1
  cupertino_icons: ^1.0.2

  event_bloc: ^4.2.0
  event_db: ^0.1.0
  # event_db:
  #   path: ../event_db
  event_hive: ^0.1.2
  # event_hive:
  #   path: ../event_hive
  event_navigation: ^0.6.2
  # event_navigation:
  #   path: ../event-navigation

  vhcblade_theme: ^0.1.1
  # vhcblade_theme:
  #   path: ../vhcblade_theme

  flutter_markdown: ^0.6.13
  
  intl: ^0.18.0
  math_parser: ^1.5.0
  provider: ^6.0.0
  tuple: ^2.0.0

  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
